class DeriveReduceInterface {
  constructor (classObj) {
    this.classObj = classObj;
    this.cancel = true;
  }
  content (element) {
    // To get attribute names:
    // Object.keys(this.classObj.table.getAttributeDetails())

    // To get connected class objects:
    // for (const connectedClass of this.classObj.connectedClasses()) {
    //
    // }
    element.append('svg');
    // element.text('custom plugin on its way');
  }
  ok () {
    console.log('clicked ok');
  }
}
export default DeriveReduceInterface;
